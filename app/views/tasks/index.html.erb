<h1>My tasks</h1>
<% @tasks.each do |task| %>
<ul>
  <%if  task.completed == true %>
  <input type="checkbox" checked="" />
  <%else %>
  <input type="checkbox" />
  <%end %>
  <a href="/tasks/<%= task.id%>" ><%= task.title %><strong> <%= task.details %></strong>
</a>
<%= link_to "🗑", destroy_path(task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
    </ul>


  <% end %>
  <br>
  <br>
  <a href="/tasks/new" >Add a new task</a>
